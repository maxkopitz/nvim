require('bufferline').setup  {
  options = {
    numbers = "buffer_id",
    tab_size = 10,
    max_name_length = 18,
    max_prefix_length = 15,
    diagnostics = false,
    sort_by = "id",
    always_show_bufferline = true,

  }
}
